<%= link_to "Go home, u drunk", root_path %>

<h1>Users</h1>
  <% @posts.each do |post| %>
    <h2><%= post.name %></h2>
    <p>from: <em><%= post.place %></em></p>
  <% end %>

<%= link_to "Go to never visited dimension", never_visited_path %>
